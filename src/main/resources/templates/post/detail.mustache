{{>/layout/main-header}}

<div class="container">
    <input id="postId" type="hidden" value="{{post.id}}" />
    <input id="pageOwnerId" type="hidden" value="{{post.user.id}}" />
    <div class="my_post_detail_title">
        <h2>{{post.title}}</h2>
    </div>
    <hr />

    <!-- 게시글 콘텐츠 시작 -->
    <div class="my_post_detail_content">
        {{{post.content}}}
    </div>
    <!-- 게시글 콘텐츠 끝 -->

    <div>
        <a class="btn btn-outline-success" href="#">수정</a>
        <button id="btn-delete" class="btn btn-outline-danger">삭제</button>
    </div>
    <br />

</div>

<script src="/js/post/detail.js"></script>

{{>/layout/footer}}